Dear user of CSManager,

You have submit a request to create a new account.<br/>
This task was completed with success.


{% if preference.getSiteBaseUrl() %}
Connect to : {{ preference.getSiteBaseUrl() }}{{ path("core_home_index") }}
{% else %}
Connect to : {{ path("core_home_index") }}
{% endif %}
